name: Test

on:
  workflow_dispatch:

jobs:
  job_1:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

  job_2:
    runs-on: ubuntu-latest
    needs: job_1

    steps:
      - run: ls
