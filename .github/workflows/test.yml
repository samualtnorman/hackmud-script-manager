name: Test

on:
- push
- pull_request

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
    - name: Setup
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v1
    - run: npm ci

    - name: Test
    - run: npm test
